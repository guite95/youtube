# .ebextensions/01_install_ffmpeg.config

# packages 섹션을 제거하고 commands로 모든 설치를 처리합니다.
commands:
  # 1. EPEL(Extra Packages for Enterprise Linux) 저장소를 설치합니다. 
  # Amazon Linux 2023에서는 dnf install epel-release가 필요합니다.
  01_install_epel:
    command: sudo dnf install -y epel-release

  # 2. EPEL이 설치된 후, dnf를 사용하여 FFmpeg를 설치합니다.
  02_install_ffmpeg:
    command: sudo dnf install -y ffmpeg